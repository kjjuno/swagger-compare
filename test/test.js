var assert = require('assert');

describe('fake tests', function() {
  it('is a fake test', function() {
    assert.equal(1,1);
  });
  it('is another fake test', function() {
    assert.equal(1,1);
  });
});
